# uv / virtual env
.venv/
__pycache__/
*.pyc

# generated EDA outputs
eda_output/

# local-only data brief (not for the repo)
data_understanding.html

# runtime nudge logs (regenerated by notify.py / campaign sim)
data/clean/notifications_log.csv
data/clean/responses_log.csv
